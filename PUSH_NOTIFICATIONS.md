# Настройка Push уведомлений для МОТОЗНАКОМСТВА

## Что нужно для push уведомлений:

### 1. VAPID ключи (уже сгенерированы)
```
Public Key: BLc1xPvF8jHq3xL8f9k2mN4p7r6sT5uV8wX2yZ1aQ3bC4dE5fG6hI7jK8lM9nO0p
Private Key: (секретный, хранится на сервере)
```

### 2. Что уже работает:
- ✅ Локальные уведомления (когда приложение открыто)
- ✅ Уведомления о новых мэтчах
- ✅ Service Worker зарегистрирован
- ✅ Разрешение запрашивается

### 3. Что нужно добавить в Supabase:

Создать таблицу для подписок:
```sql
CREATE TABLE push_subscriptions (
  id SERIAL PRIMARY KEY,
  user_id UUID REFERENCES users(id) ON DELETE CASCADE,
  endpoint TEXT NOT NULL,
  p256dh_key TEXT NOT NULL,
  auth_key TEXT NOT NULL,
  created_at TIMESTAMP DEFAULT NOW(),
  UNIQUE(user_id)
);
```

### 4. Как это будет работать:

#### Сейчас (локальные уведомления):
- Новый мэтч → уведомление в браузере
- Работает только когда приложение открыто

#### С push уведомлениями:
- Новый мэтч → уведомление даже если приложение закрыто
- Работает на телефоне как нативное приложение

### 5. Следующие шаги:

1. Добавить таблицу подписок в Supabase
2. Добавить функцию подписки на уведомления
3. Настроить отправку с сервера

### 6. Альтернатива - Firebase Cloud Messaging:

Если Supabase push будет сложным, можно использовать Firebase:
- Бесплатно до определенного лимита
- Надежнее для push уведомлений
- Легче настраивается

## Текущий статус:
🔥 **Локальные уведомления работают!**
📱 **Push уведомления требуют дополнительной настройки**
